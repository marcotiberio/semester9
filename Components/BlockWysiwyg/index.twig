<flynt-component client:visible name="BlockWysiwyg" class="">
  <div class="container boxed">
    <div class="content {{- options.maxWidth != 'full' ? ' contentMaxWidth'}}{{ options.maxWidth == 'large' ? 'Large' }}" {{ textAlign ? 'data-text-align="' ~ textAlign ~ '"' }}>
      {{ contentHtml|e('wp_kses_post') }}
    </div>
  </div>
</flynt-component>
